# Direct runtime dependency lock for FA3-BLACKHOLE-API-001.
# The OCI admission collector records the fully resolved pip freeze and image digest as evidence.
fastapi==0.141.1
pydantic==2.13.5
uvicorn==0.52.4
