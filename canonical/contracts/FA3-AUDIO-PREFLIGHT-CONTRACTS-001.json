{
  "id": "FA3-AUDIO-PREFLIGHT-CONTRACTS-001",
  "schema": "fa3.audio-preflight-contracts.v1",
  "status": "CANONICAL",
  "provider_neutral": true,
  "layers": [
    {
      "id": "L1_INPUT_INTEGRITY",
      "requires": ["artifact_id", "sha256", "codec", "sample_rate_hz", "channels"]
    },
    {
      "id": "L2_MEDIA_CONTRACT",
      "requires": ["supported_codec", "supported_sample_rate", "supported_channel_count"]
    },
    {
      "id": "L3_RESOURCE_BUDGET",
      "requires": ["ram_bytes", "vram_bytes", "estimated_peak_bytes", "bounded_execution"]
    },
    {
      "id": "L4_HRB_ADMISSION",
      "requires": ["accelerator_requires_hrb_lease", "provider_cannot_self_place_device"]
    },
    {
      "id": "L5_PROVENANCE",
      "requires": ["source_artifact_id", "provider_id", "model_or_runtime_lock_ref"]
    },
    {
      "id": "L6_OUTPUT_CONTRACT",
      "requires": ["output_artifact_type", "output_codec", "output_lineage_required"]
    }
  ],
  "policy": {
    "fail_closed": true,
    "provider_specific_gate_still_required": true,
    "pydantic_or_api_schema_validation_is_not_sufficient": true,
    "resource_overcommit_forbidden": true,
    "accelerator_without_hrb_lease_forbidden": true,
    "missing_provenance_forbidden": true
  }
}
