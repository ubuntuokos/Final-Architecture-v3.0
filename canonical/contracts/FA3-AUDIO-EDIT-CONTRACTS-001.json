{
  "schema": "fa3.contract-record.v1",
  "id": "FA3-AUDIO-EDIT-CONTRACTS-001",
  "version": "1.1.0",
  "status": "CANONICAL",
  "contracts": {
    "AudioEditAIRequest": {"required": ["operation", "input_artifact", "selection", "requested_capability", "policy_context", "resource_request"], "operations": ["denoise", "transcribe", "separate", "enhance", "generate"]},
    "AudioEditResourceRequest": {"required": ["cpu", "memory", "accelerator_required"], "accelerator_optional_fields": ["capability_requirements", "memory_requirement"], "semantics": "WORKLOAD_DERIVED_NOT_HOST_CONSTANT"},
    "AudioEditAIResult": {"required": ["output_artifacts", "track_mapping", "provider_identity", "model_identity", "parameters", "provenance", "admission_receipt"]},
    "AudacityAICompatibilityAdmission": {"required": ["audacity_version", "module_api_abi", "runtime_version", "model_compatibility"], "accelerator_fields_required_only_when_accelerator_requested": ["device_capability", "hrb_lease", "accelerator_guard_reconciliation"], "pass_condition": "compatibility pass AND HRB admission pass AND accelerator lease/guard pass when accelerator requested", "failure": "deny affected provider workload only"}
  },
  "invariants": ["AI outputs MUST be explicit artifacts/tracks/labels", "Every AI transformation MUST carry provider/model/parameter provenance", "CPU-only execution MUST NOT require implicit accelerator discovery or lease", "Accelerator execution MUST require HRB lease and Accelerator Guard reconciliation", "Fixed CPU/GPU/NPU/NUMA values are forbidden", "Implicit display-to-compute or accelerator fallback is forbidden", "Compatibility/resource admission failure MUST be fail-closed for the affected provider workload", "Routing and host resource placement remain FA3 authorities"]
}
