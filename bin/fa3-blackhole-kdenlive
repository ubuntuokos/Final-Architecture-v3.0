#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHON="${FA3_BLACKHOLE_PYTHON:-python3}"
export PYTHONPATH="${ROOT}/src${PYTHONPATH:+:${PYTHONPATH}}"
exec "$PYTHON" "${ROOT}/src/fa3_blackhole_kdenlive.py" --root "${ROOT}" "$@"
