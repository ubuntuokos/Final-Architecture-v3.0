# FA3 Browser Action Runtime\n\n`FA3-BROWSER-ACTION-RUNTIME-001` is a P0 subprofile of `FA3-WEB-AI-001`. It adds no capability or architectural authority. It adopts selected MIT-licensed patterns from `browser-use/jev-ultrafast` pinned at `1231850a0bf1a0c0341fe408ef1668dbbfdfac46`.\n\n## Lifecycle\n\n`OBSERVE -> NORMALIZE -> INDEX -> BUILD_BOUNDED_ACTION_SET -> DECIDE -> REVALIDATE -> AUTHORIZE -> EXECUTE -> VERIFY -> EVIDENCE -> OBSERVE_AGAIN`\n\nPage content is always `UNTRUSTED_EXTERNAL_CONTENT`. A decision may select only an action already emitted by the runtime. Observation ID, revision, document ID, page fingerprint and action-space hash are execution-bound; stale decisions fail closed.\n\n`DONE` is only a completion claim. Success requires independent postcondition verification. Mutating operations are non-idempotent and cannot be blindly retried. `TYPE_TEXT` text generation is a separate request on the central Model Router logical route; Jev cannot select another model or provider.\n\nCPU-only operation is mandatory. NVIDIA, CUDA, a specific accelerator, Browser Harness and external Jev service are optional implementation details, not canonical dependencies.\n\nNo upstream runtime source is vendored by this materialization; provenance, MIT attribution, security review and distribution review are recorded in `FA3-JEV-CODE-REUSE-001`.\n